<% @quizzes = Quiz.where(user_id: quiz.user[:id])%>
<h1 style="text-align: center">Quizzes for Today</h1>
<%= debug @current_user %>
<table class="table">
  <thead>
    <tr>
      <th>Quiz name</th>
      <th>Created by </th>
     <!-- <th></th>
       <th></th>
     <th></th> -->
    </tr>
  </thead>

  <tbody>
    <% @quizzes.each do |quiz| %>
      <tr>
        <td><h3><%= link_to quiz.title, quiz %></h3></td>
        <td><%= quiz.user["username"] %></td>
       <!-- <td><%= link_to 'Show', quiz %></td>
         <td><%= link_to 'Edit', edit_quiz_path(quiz) %></td>
       <td><%= link_to 'Destroy', quiz, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<div style="text-align: center"><%= link_to 'Create Your Own Quiz', new_quiz_path, class: "btn btn-primary btn-lg" %></div>