<%= form_for(@user, :html=>{:class => "form-horizontal col-sm-3 float-middle"}) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.text_field :username, class: "form-control col-sm-5", placeholder: "Username" %>
  </div>
  <div class="field form-group">
    <%= f.password_field :password, class: "form-control col-sm-5", placeholder: "Password" %>
  </div>
  <div class="field form-group">
    <%= f.password_field :password_confirmation, class: "form-control col-sm-5", placeholder: "Password Confirmation" %>
  </div>
  <div class="actions">
    <%= f.submit "Register", class: "btn btn-primary btn-default"  %>
  </div>
<% end %>
